class EmployeeAdd{
  final String firstName;
  final String phone;
  final String email;
  final String client;
  final String vendor;
  final String payrate;
  final String frequency;

  const EmployeeAdd(this.firstName, this.phone, this.email, this.client, this.vendor,
      this.payrate, this.frequency);
}